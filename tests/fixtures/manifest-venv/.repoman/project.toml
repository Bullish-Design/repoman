# Flake-check fixture: a repository that selects only its core managers.
# The check in flake.nix evaluates the real module against this root and
# asserts the roster resolves from the manifest, with no devenv.nix option set.
schema = 1
managers = ["copy", "git"]
