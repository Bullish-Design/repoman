[pytest]
markers =
    integration: Integration tests that require network and git
    slow: Slow tests that take more than a few seconds

asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
